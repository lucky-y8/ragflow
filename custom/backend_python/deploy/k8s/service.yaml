apiVersion: v1
kind: Service
metadata:
  name: ragflow
  labels:
    app.kubernetes.io/name: ragflow
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ragflow
  ports:
    - name: http
      port: 80
      targetPort: http
